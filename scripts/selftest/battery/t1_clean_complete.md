MISSION BRIEF (user-approved — harness battery test T1: the clean-completion path). You own this goal end-to-end. ZERO cost, no GPU, no workers (do the small work yourself inline — a mission is ownership, not size). Scratch dir: `.vault-chat/selftest/battery/`. Goal file: `.vault-chat/supervisor/goals/selftest-t1.md`.

Mission: SELFTEST T1 — clean completion path

Done when:
- The file `.vault-chat/selftest/battery/t1_result.md` exists containing a 5-line inventory: the number of goal files under `.vault-chat/supervisor/goals/`, the three most recently modified ones by name, and today's date.
- One RecordJudgment entry for this mission exists in the decision log (a genuine call you made while working — e.g. what you chose to include in the inventory and why).

Work inline, put the concrete evidence in mind.md (file path, line count, a quoted line) BEFORE claiming, MarkDoneWhen each criterion, then CompleteMission with a one-line wrap-up. This test passes if you reach DONE with both criteria verified — no shortcuts, no ceremony.
